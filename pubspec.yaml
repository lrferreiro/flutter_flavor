name: flutter_flavor
description: Easy and fast configuration of flavors in your Flutter App.
version: 0.0.1
homepage: https://github.com/lrferreiro/flutter_flavor

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter